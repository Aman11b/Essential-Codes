* -> Edit package.json > Add this at top level:
    "homepage": "https://aman11b.github.io/eat-n-split",

    Inside "scripts":

    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"

* -> npm install gh-pages --save-dev
* -> npm run deploy
